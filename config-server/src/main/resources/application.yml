server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jzingler/apps-spring-cloud-services-labs-config
          
          
management:
  security:
    enabled: false
